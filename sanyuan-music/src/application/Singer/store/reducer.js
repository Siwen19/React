const defaultState = {
    category: "",
    alpha: "",
    singerList: [],
    enterLoading: true,
    // 分页
    listOffset: 0
}

export default (state = defaultState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}
